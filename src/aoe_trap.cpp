
#include "aoe_trap.h"
#include <godot_cpp/core/class_db.hpp>

using namespace godot;

/**
 * Bind the methods of AoeTrap so they can be called from Godot or via RPC.
 * Currently no methods are exposed; this placeholder keeps the binding
 * consistent for later porting.
 */
void AoeTrap::_bind_methods() {
}

/** Default constructor for AoE trap skeleton. */
AoeTrap::AoeTrap() {}

// Default destructor.
AoeTrap::~AoeTrap() {}

// Called when the node enters the scene tree. Use to initialize state.
void AoeTrap::_ready() {}

// Per-frame processing. 'delta' is seconds since last frame.
void AoeTrap::_process(double delta) {}
