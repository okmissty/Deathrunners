#include "death_controller.h"
#include <godot_cpp/core/class_db.hpp>

using namespace godot;

/** Bind DeathController API to Godot. */
void DeathController::_bind_methods() {
}

/** Construct a DeathController (handles trap activation UI/logic). */
DeathController::DeathController() {
}

/** Destructor. */
DeathController::~DeathController() {
}

/** Called when controller enters scene tree. */
void DeathController::_ready() {
}

/** Per-frame updates for DeathController. */
void DeathController::_process(double delta) {
}
